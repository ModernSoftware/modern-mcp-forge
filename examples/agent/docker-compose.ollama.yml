services:
  ollama:
    image: ollama/ollama:latest
    container_name: modern-mcp-forge-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama-data:/root/.ollama
    restart: unless-stopped

volumes:
  ollama-data:
